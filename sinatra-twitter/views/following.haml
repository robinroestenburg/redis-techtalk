#timeline.content-main
  .content-header
    .header-inner
      %h2 Following
  .stream-container
    .stream.home-stream
      .stream-items
        - @followees.each do |followee|
          .stream-item
            .account
              .user-actions.btn-group.not-following
                %button.btn-primary.btn{:type => "button"}
                  Follow

              .content
                .stream-item-header
                  %a{:class => "account-group js-account-group js-action-profile js-nav", :href => "/#{followee.username}"}
                    %strong.fullname.js-action-profile-name= followee.fullname
                    %span.username.js-action-profile-name<
                      %s @
                      %b= followee.username

      .stream-footer
        .timeline-end
          .stream-end
            .stream-end-inner
